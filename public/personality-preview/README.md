# 免费人格形象预览

免费版独立预览图片放在本目录，命名为 `type01.webp` 至 `type16.webp`。

预览图必须是从正式IP原图单独导出的裁切、低清或被构图遮挡版本，不能只是让浏览器下载完整高清原图后使用 CSS 模糊。

当前已从V3正式源图生成 `type01.webp` 至 `type16.webp`：统一为320×400低清WebP，并进行模糊、轻降饱和处理。页面层继续通过局部放大和遮挡制造不完整预览。

禁止用完整原图覆盖这些文件，也不要仅依靠CSS隐藏高清资源。
